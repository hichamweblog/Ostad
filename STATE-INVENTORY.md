# جرد الحالة والأجزاء القابلة للتغيير

هذه الوثيقة هي مرجع المراجعة قبل أي تغيير في تطبيق «معين الأستاذ». كل بيانات
المستخدم القابلة للحفظ يجب أن تملك مساراً واضحاً إلى Supabase، أو إلى
IndexedDB outbox عند العمل دون اتصال. حالات الواجهة المؤقتة لا تُرفع إلى
Supabase.

## مصادر الحالة

| المصدر | المحتوى | قاعدة التعامل |
| --- | --- | --- |
| `AppState` في `lib/storage.ts` | بيانات الأستاذ والأقسام والتلاميذ والتدريس والتقويم والإعدادات والملفات | مصدر عقد الواجهة؛ Supabase مصدر الحقيقة السحابي |
| `useCloudAppState` | الحالة الموحدة، حالة السحابة، أخطاء المزامنة والحفظ المحلي | مالك دورة التحميل والحفظ والمزامنة |
| `state-cache.ts` | نسخة محلية من `AppState` | Cache لا ينافس Supabase |
| `sync-outbox.ts` | عمليات upsert/delete للكيانات | لا تُحذف قبل الإقرار |
| `memoranda-outbox.ts` | عمليات رفع/حذف PDF | يضمن استمرارية الملفات دون اتصال |
| `localStorage` | معرّف الجهاز فقط | ممنوع تخزين بيانات المستخدم |

### حدّ المجال typed

يملك `useCloudAppState` وحده دورة التحميل والحفظ والمزامنة، ويُمرّر عقده إلى
`AppStateProvider` في `hooks/app-state-context.tsx`. تستهلك مكونات مساحة العمل
`useAppState()` typed للحصول على `state` و`updateState` وعمليات الاستبدال
والإعادة الضبط، بدلاً من تمرير `AppState` أو `onUpdateState` عبر Props. هذا
حدّ وصول فقط ولا يضيف مخزناً أو دورة مزامنة جديدة؛ كل التغييرات ما زالت تمر عبر
`useCloudAppState` ثم cache/outbox وSupabase/Realtime نفسها. الحالة المؤقتة
للنوافذ والتنقل تبقى محلية للمكونات، ولا تدخل السياق.

`updateStateAndWait` يطبق التغيير محلياً فوراً ويمنح المزامنة السحابية مهلة قصيرة
للتأكيد. إذا تأخر التأكيد، تبقى العملية في outbox وتُعاد المحاولة تلقائياً دون
حذفها، ويستمر مسار الواجهة دون انتظار طويل؛ لا تُكشف تفاصيل الطابور الداخلية.

قبل أي enqueue أو flush تتحقق دورة المزامنة من أن جلسة Supabase الحالية
مصادق عليها وأن `auth.uid()` يطابق مالك العملية. عند `SIGNED_OUT` تُلغى
مؤقتات الحفظ وتُغلق دورة المستخدم السابق، وتبقى عناصر outbox محلية دون
محاولة كتابة مجهولة؛ تُعاد مزامنتها فقط بعد جلسة صحيحة جديدة.

يعالج `AuthGate` وصول كود OAuth إلى `/` (وهو مسار قد تستخدمه إعدادات Supabase
الحالية) بتحويله مرة واحدة إلى `/auth/callback`، حيث يتم تبادل الكود خادمياً.
لا يُخزّن الكود في الحالة أو IndexedDB ولا يُعاد إرساله بعد استهلاكه.
يستخدم بدء OAuth وcallback أصل الصفحة/الطلب الحالي نفسه لضمان بقاء كوكي PKCE
على النطاق ذاته؛ لا يُستخدم عنوان Vercel canonical مختلف لهذا التحويل.
وإذا أعاد Supabase callback إلى نطاق Vercel الافتراضي، يعيد callback توجيه
الكود غير المستخدم إلى الأصل العام قبل تبادله، بدلاً من محاولة التبادل على
نطاق لا يملك كوكي PKCE.

### حل التعارضات

تسجل تعارضات revision في `sync_conflicts` بالحالة `pending`، ويُنشأ وصف typed
من عملية Outbox الخاصة بالمالك. تبقى النسخة المحلية في Outbox ولا تُكرر في
Supabase، بينما تُحمّل النسخة السحابية الحالية من صفها المملوك للمستخدم قبل
عرضها. الاحتفاظ بالسحابية يحذف عملية Outbox المتعارضة فقط ثم يعيد تحميل الحالة.
الاحتفاظ بالمحلية ينشئ operation IDs جديدة وrevision أحدث من السحابية ويمررها
عبر مسار Outbox المعتاد. لا تتجاوز إعادة المحاولة التعارض المعلّق تلقائياً.

## كيانات الأعمال القابلة للتعديل

| الحالة | جدول/خدمة Supabase | الحذف/التعارض |
| --- | --- | --- |
| `profile` | `profiles` (الأسماء العربية/الإنجليزية، البريد، الهاتف، بيانات التعيين/الميلاد/الأسرة/الجنس) | metadata للمراجعة؛ الصورة تحتاج Storage/outbox؛ trigger لتحديث `sync_updated_at` |
| `calendarSettings`, `theme`, `dashboardStyle`, `sidebarCollapsed`, `onboardingDismissed`, `activeClassId`, `activeTrimester` | `app_settings` | revision وconflict؛ trigger خادمي موحد |
| `classes` | `classes` (`color` persisted) | tombstone |
| `students` | `students` | tombstone؛ الاستيراد الجماعي يمر عبر `import_roster_batch` مع outbox كضمان لاحق |
| `timetable` | `timetable_slots` | tombstone؛ تغيير القسم النشط يغيّر العرض/الاختيار فقط ولا يحذف أو يعيد كتابة صفوف التوقيت |
| `sessions` | `sessions` (`summary` و`assignments` لعقد محرر الحصة المختصر) | tombstone؛ يرتبط بالحضور والسلوك؛ التعديل يمر عبر نفس revision/outbox/conflict |
| `attendance` | `attendance` | tombstone علائقي مستقل |
| `session behaviors` | `session_behaviors` | tombstone علائقي مستقل |

### حدود التنقل

مسار `/classes` يعرض الأقسام المسندة فقط، ومسار `/students` يملك واجهة
قوائم التلاميذ والاستيراد. أما استعمال الزمن الأسبوعي فمملوك لمسار `/timetable`
وواجهة جدول التوقيت الموحدة؛ لا توجد نسخة ثانية من الجدول في القائمة الجانبية.
تغيير المسار أو `activeClassId` لا يغيّر مصدر الحقيقة ولا يحذف بيانات أي قسم.
| `grades` | `grades` | revision؛ الحسابات المشتقة لا تصبح مصدراً ثانياً |
| `lessonProgress` | `lesson_progress` | tombstone؛ حالة الإنجاز في المنهاج والتوزيع السنوي تُشتق تلقائياً من `lesson_progress` أو من جلسات `sessions` الموثقة دون إنشاء مخزن ثانوي |
| `customUnits` | `custom_units` | tombstone |
| `lessonPlans` | `lesson_plans` | tombstone |
| `dashboardTasks` | `dashboard_tasks` | tombstone |
| `unitPdfFiles` | `memoranda_files` + Storage bucket `memoranda` | outbox للرفع والحذف؛ `unit_key` للوحدات الرسمية؛ trigger لتحديث revision |
| `deletedRecordIds` | `sync_tombstones` عند الإرسال | يمنع resurrection من نسخة أقدم |

## حالات واجهة لا تُزامَن

تشمل: فتح النوافذ والحوارات، التبويبات، البحث والفلاتر، النصوص المؤقتة في
النماذج، رسائل النجاح والخطأ، العنصر الجاري حذفه، حالة تحليل ملف Excel،
حالة تحميل المنهج/PDF، وحالات `isOnline` وPWA install وdebounce وToast.

## مسارات التعديل التي يجب تدقيقها

1. CRUD الأقسام والتلاميذ والجدول والجلسات.
2. الحضور والسلوك والعلامات والحسابات.
3. الوحدات المخصصة والتقدم وخطط الدروس.
4. مهام لوحة التحكم.
5. الملف الشخصي والإعدادات والعطل والتفضيلات.
6. استيراد الرقمنة والممتاز والدمج والنسخة الاحتياطية.
7. رفع واستبدال وحذف PDF والطباعة والتصدير.
8. reset workspace والحذف العلائقي وإعادة المحاولة والتعارض.

## مصفوفة التغطية الحالية

| المجال | Supabase | outbox | Realtime | ملاحظة |
| --- | ---: | ---: | ---: | --- |
| الكيانات الدراسية والتدريسية | نعم | نعم | نعم | يحتاج اختبارات مجال أوسع |
| attendance/behavior/tasks | نعم | نعم | نعم | tombstones موجودة |
| profile/settings | نعم | نعم | نعم | يحتاج اختبار تعارض خاص |
| PDF metadata وStorage | نعم | نعم | metadata | الرفع والحذف واستعادة metadata تنتظر تأكيد السحابة؛ يحتاج اختبار تكاملي |
| Excel imports | نعم | نعم | نعم عبر الصفوف الناتجة | RPC ذري `import_roster_batch` مع fallback إلى outbox |
| avatar binary | مسار Storage | نعم | لا | `avatars` bucket مع outbox محلي؛ يعاد إنشاء signed URL عند التحميل، وتدعم الإزالة outbox |
| retry/backoff | نعم | نعم | لا | محاولة مؤجلة بتزايد أسي حتى 5 دقائق مع حفظ attempts/nextAttemptAt |
| operation idempotency | operation id حتمي لكل revision | نعم | نعم | ledger `sync_operations` وRPC `claim_sync_operation` يمنعان تكرار العملية نفسها بعد انقطاع الاستجابة دون حجب تحديثات لاحقة |
| reset workspace | `reset_workspace` + RLS/FK | تنظيف outbox/cache بعد الإقرار | إعادة تحميل لاحقة | يحذف ملفات memoranda/avatar وسجل العمليات؛ و`dashboard_tasks` يملك trigger مزامنة واحداً |
| Excel import/export | Supabase RPC للدفعة الذرية | الحالة المحلية ثم outbox عند الفشل | إعادة تحميل الصفوف بعد الإقرار | تحميل `xlsx` وparsers ديناميكياً عند فتح الاستيراد/التصدير |

## نتيجة التدقيق المباشر الأخير

- الجداول الأربعة عشر مفعّل عليها RLS، ولكل جدول سياسة owner، وثلاثة أعمدة
  `sync_revision`/`sync_updated_at`/`sync_device_id`.
- الجداول الأربعة عشر مضافة إلى `supabase_realtime`.
- تم تفعيل نظام **المزامنة التفاضلية (Delta Sync Outbox)** عبر `getSyncOperationsDelta` و`enqueueSyncDelta`؛ بدلاً من رفع وتحديث كامل مصفوفات البيانات (أكثر من 500 كيان) عند كل تعديل بسيط، يتم إرسال الكيانات المعدلة أو المحذوفة فقط إلى طابور Outbox، مما يخفض استهلاك الشبكة بنسبة تفوق 98% ويمنع الاختناق (HTTP request storm).
- تم ضبط **ساعة المزامنة السحابية (Revision Clock Alignment)** عبر اشتقاق `cloudRevision` من أعلى revision في قاعدة بيانات Supabase عند التحميل (`loadCoreState`) وتحديث `revisionRef.current` محلياً، مما يقضي نهائياً على التعارضات الكاذبة (`SyncConflictError`) عند إعادة تحميل الصفحة أو التبديل بين الأجهزة.
- تم تمديد صلاحية روابط التحميل الموقعة (Signed URLs TTL) لكل من الصور الرمزية والمذكرات (`avatars` و`memoranda`) من ساعة واحدة (3600 ثانية) إلى 7 أيام (604,800 ثانية) لضمان استمرارية فتح المذكرات والملفات في جلسات PWA الطويلة دون انقطاع.
- تم تفعيل **إلغاء صدى التحديثات (Realtime Echo Cancellation)** عبر `sync_device_id`؛ بحيث يتجاهل العميل إشعارات Supabase Realtime الصادرة منه ذاتياً، متجنباً إعادة جلب الجداول الـ 14 كاملة عبر `SELECT *` بعد كل عملية حفظ ناجحة.
- تم تفعيل triggers خادمية موحدة لكل الكيانات التي تحتوي `revision`، وtrigger
  خاص لـ`profiles`، حتى لا تتغير الصفوف من مسار خادمي دون تحديث ساعة المزامنة.
- تم تصحيح تسجيل التعارض ليستعمل UUID السحابي بدلاً من المعرّف المحلي.
- تم منح دور `authenticated` صلاحيات Data API الصريحة على الجداول العلائقية
  مع إبقاء RLS حدّ التفويض، حتى لا تفشل قراءة `memoranda_files` أو أي كيان
  آخر برسالة `42501`.
- تمّت معالجة الحسابات المنشأة قبل trigger التسجيل بإنشاء `profiles` و
  `workspaces` الناقصة لها؛ والتحقق الحالي يثبت عدم وجود مستخدم بلا مساحة عمل.
- يمرّر `exportToDoc` مخرجات HTML عبر `sanitizeExportColors` قبل التنزيل، فتحافظ
  قوالب Word على ألوانها الدلالية دون ترك قيم Hex داخل HTML الناتج.
- يملك التطبيق الآن حدود تحميل وخطأ وعدم العثور الجذرية، إضافة إلى الحدود
  المستقلة الموجودة للمسارات العملية؛ حالات التحميل لا تدخل دورة المزامنة.
- إعادة تعيين الأقسام والتلاميذ تمر الآن عبر تنفيذ سحابي مباشر وذري `clearCloudRosterData`
  (عبر دالة قاعدة البيانات `clear_roster_data()` مع fallback مباشر لحذف الجداول المعنية)،
  مع تنظيف شامل لطابور outbox قبل وبعد العملية لمنع تراكم مئات العمليات المتسلسلة
  والاختناق، وكتابة الحالة الصفرية فوراً إلى الذاكرة والكاش وتحديث حالة المزامنة إلى `ready`.
  يضمن ذلك مسح البيانات بشكل قطعي وفوري دون بقائها عالقة في طابور المزامنة أو عودتها عند العودة إلى لوحة التحكم.
- تم تحسين حساب المحذوفات `getDeletedRecordIds` و`getSyncOperationsDelta` لتفادي طوفان
  الشواهد (Tombstone Flooding)؛ فعند حذف قسم كامل، لا يتم توليد عشرات عمليات الحذف
  المنفصلة لتلاميذ وحصص وعلامات ذلك القسم لأن قاعدة البيانات تتكفل بحذفها تلقائياً عبر
  قيود الشلال العلائقي (`ON DELETE CASCADE`).
- تم تفعيل التصفية العلائقية في `loadCoreState` لضمان عدم استبقاء أي سجلات يتيمة
  (تلاميذ، علامات، جلسات، حصص توقيت، تقدم منهاج) في حال غياب القسم التابعين له.
- إعدادات التقويم واستعادة أرشيف PDF لا تعرض النجاح قبل اكتمال
  `updateStateAndWait`، كما أن رفع وحذف مذكرة PDF ينتظران Storage وmetadata
  السحابيين قبل تحديث واجهة النجاح.
- تحذير Supabase الوحيد المتبقي هو Leaked Password Protection الخارجي
  المستبعد بطلب المستخدم.
- حالة المزامنة تعرض الآن في `TopHeaderSanad` مع إجراء إعادة المحاولة، إضافة إلى
  مؤشر التفاصيل السفلي.

## قاعدة التغيير

قبل تعديل أي جزء:

1. حدّد هل هو بيانات أعمال أم حالة واجهة مؤقتة.
2. حدّد مصدر الحقيقة وجدول Supabase أو سبب بقائه محلياً.
3. حدّد upsert/delete وoutbox وRealtime وconflict/tombstone.
4. أضف اختباراً أو تحققاً مناسباً.
5. حدّث هذه المصفوفة إذا تغيّر نطاق التغطية.

- نجح `npm run lint` (0 أخطاء و0 تحذيرات).
- نجح `npm run test -- --run` بعدد 4 ملفات و53 اختباراً (نجاح كامل وشامل للـ delta sync وdate/time normalization وconflict handling وuniversal non-destructive retention وatomic roster reset).
- نجح `npm run build` باستخدام Webpack.
- نجح `git diff --check`.
- تم تفعيل **المعالجة الآمنة للتواريخ والأوقات (`normalizeDateToIso` و`normalizeTime`)**: تحويل كافة صيغ التواريخ الجزائرية والأوقات (DD/MM/YYYY، الأرقام العربية المشرقية، الأرقام التسلسلية لـ Excel، صيغ الوقت ذات الثواني أو من خانة واحدة) إلى معيار ISO و24-Hour HH:MM، مع تفادي أخطاء PostgreSQL 22008 وصيغ time غير الصالحة.
- تم ضبط **الاستبقاء غير الهدام الشامل (Universal Non-Destructive Retention)** في `loadCoreState` لكافة الكيانات التسعة: الأقسام (`classes`)، التلاميذ (`students`)، التقييم والنقاط (`grades`)، دفتر النصوص والجلسات (`sessions`)، جدول التوقيت (`timetable`)، تقدم المنهاج (`lessonProgress`)، الوحدات المخصصة (`customUnits`)، مذكرات الدروس (`lessonPlans`)، والمهام اليومية (`dashboardTasks`). هذا يضمن عدم ضياع أي عنصر محلي تم إدخاله دون اتصال أو قيد الإرسال عند التنقل بين المسارات.
- تم تفعيل **حماية قيود الجداول وفحص الشروط (Check Constraint Guards)**:
  - ضبط ومطابقة حالات تقدم المنهاج (`status in ('not_started','in_progress','completed')`) لمنع تعارض الحالات الكبيرة مثل `COMPLETED` أو `IN_PROGRESS` مع قاعدة البيانات السحابية.
  - تحجيم وضبط علامات التقييم المستمر والاختبارات والفروض (`continuous_eval`, `quiz`, `exam`) ضمن النطاقات المسموحة رسمياً (0-20 للنقاط و0-5 لعناصر التقويم، و1-3 للفصول).
  - ضمان صحة ترتيب وقت البداية والنهاية للحصص وجداول التوقيت (`end_time > start_time`).
  - توفير قيم افتراضية غير فارغة لعناوين الوحدات المخصصة ومذكرات الدروس لمنع انتهاك شرط `length(btrim(title)) > 0`.
- تم تصحيح حمولة استيراد القوائم الجماعية (`commitRosterImportBatch`) ومطابقتها مع المعاملات المتوقعة لـ `import_roster_batch` بدعم مفاتيح camelCase وsnake_case معاً.
- تم ضبط **استبقاء الحالة في الذاكرة (In-Memory Module Retention)** عبر مسارات Next.js App Router في `useCloudAppState` لمنع تصفير الحالة عند التنقل بين الشاشات (`/classes` و`/students`).
- تم تفعيل استرجاع الكاش من IndexedDB عند الإقلاع للمستخدمين المصادق عليهم وحماية الكاش من الكتابة فوقه بحالة فارغة غير مهيأة.
- استيرادا الرقمنة والممتاز ينتظران إقرار `commitRosterImportBatch` قبل رسالة
  النجاح؛ عند الفشل تبقى الحالة المحلية قابلة لإعادة المحاولة ولا تُعرض كحفظ سحابي ناجح.
- تم تنقيح جميع رسائل وإشعارات النظام للمستخدم لتعتمد التسميات المهنية السحابية ("السحابة"، "الخادم السحابي"، "المزامنة السحابية").
- تم تفعيل **الإغلاق الفوري للنوافذ والحوارات (0ms Instant Modal Dismissal)**: إغلاق نماذج إضافة وتعديل الأقسام، التلاميذ، حصص التوقيت، والوحدات، ومربعات حوار تأكيد الحذف فورياً بمجرد تأكيد المستخدم مع تطبيق التحديث المتفائل، مما قضى نهائياً على التجميد المؤقت للواجهة.
- تم تحقيق **الانتقال الفوري والسلس بين المسارات (Instant Page Transitions)**: إلغاء وميض شاشة التحميل الكاملة "جاري التحميل..." عند التنقل بين المسارات المختلفة، والاعتماد التام على البيانات المتاحة في الذاكرة والكاش.
- تم تفعيل **الحصانة السحابية لاستيراد القوائم (Resilient Cloud Roster Import)**: تطبيع كافة معرّفات الأقسام والتلاميذ إلى صيغ UUID صالحة وحتمية عبر `getCloudRecordId` لمنع أخطاء البنية في قاعدة البيانات، مع تفعيل خطة بديلة للرفع الدفعي المباشر إلى جدولي `classes` و`students` (بدفعات من 100 سجل)، مما يضمن رسوخ البيانات في السحابة حتى لو قام المستخدم بمسح بيانات الموقع محلياً (Clear site data).
- تم **فك حظر محرك المزامنة (Unblocked Sync Engine Recovery)**: السماح بإعادة محاولة المزامنة التلقائية بمجرد قيام المستخدم بأي تعديل جديد حتى لو كانت الحالة السابقة `sync-failed`، مع تقليص مهلة انتظار الإقرار إلى 2.5 ثانية بدلاً من 8 ثوانٍ.
- تم حل مشكلة **امتلاء مساحة التخزين المحلية (`QuotaExceededError`)**:
  - تصفية وتنظيف كائن الحالة قبل الحفظ في IndexedDB (`sanitizeStateForCache`) بتجريد البيانات الثنائية الضخمة (`fileDataUrl` وسلاسل base64 الكبيرة للصور والمذكرات) مما قلص حجم الكاش بأكثر من 95% وجعل حجمه لا يتجاوز مئات الكيلوبايت.
  - آلية تنظيف تلقائي للبيانات المؤقتة القديمة (`cleanupStaleStorage`) مع إعادة المحاولة وخطة بديلة لحفظ الكيانات الأساسية (الأقسام، التلاميذ، العلامات) حتى تحت أشد قيود الذاكرة في المتصفح.
  - تصفير فوري لرسالة الخطأ `setLocalStorageError(null)` بمجرد نجاح أي عملية حفظ كاش تالية.
- تم تحصين **ثبات قوائم التلاميذ السحابية ومنع ضياعها بعد تسجيل الخروج**:
  - في `commitRosterImportBatch`، البحث عن الأقسام السابقة بنفس الاسم وإعادة استخدام معرّفاتها لتفادي تعارض القيد الفريد `(workspace_id, name, academic_year)`.
  - تفريغ التلاميذ السابقين للأفواج المستوردة قبل إدراج الدفعة لتفادي التعارض مع قيد الترقيم الفريد `(workspace_id, class_id, number_in_list)`.
  - توفير مسار استرجاع وتوليد تلقائي لمساحة العمل `workspaces` عند غيابها لضمان عدم رفض عمليات الإدراج السحابي.
- تم تصحيح **سلوك إشعارات التقديرات في دفتر النقاط (`GradesAndEvaluation`)**:
  - فصل حالة إشعار النجاح عن مؤشر الحفظ التلقائي في الخلفية (`saveStatus === 'pending'`)؛ بحيث لا تظهر رسائل النجاح مطلقا باللون الأحمر أو برمز التحذير أثناء سريان الحفظ.
- تم تحسين **مؤشر عدم الاتصال (`OfflineIndicator`)**:
  - عرض أيقونة دوران المزامنة بدلاً من سحابة حمراء مشطوبة أثناء الحفظ العادي `sync-pending`، لمنع ارتباك المستخدم.
- تم تفعيل **التصفية البيداغوجية لجداول برنامج الممتاز (Moumtaze Pedagogical Filtering)**: استبعاد أوراق النتائج والتوجيه والمداولات الإدارية (`النتائج النهائية`، `توجيه الأوائل`، `قبل الإستدراك`، والأعمدة المحتوية على `معدل`/`توجيه`) التي تدرجها الإدارة أحياناً داخل مصنف الممتاز، ومنع دمج أوراق التوجيه القديمة مع الأفواج النشطة، مما ضمن دقة عدد الأقسام والتلاميذ (10 أقسام و307 تلميذاً مطابقاً للقوائم الحقيقية دون دمج خاطئ).
- تم تفعيل **تصحيح تضارب المستوى الدراسي (Class Level Reconciliation)**: معالجة الأخطاء المطبعية في كشوف الممتاز (مثل كتابة 3رياضيات داخل ورقة 2ر) بالاعتماد على رقم المستوى المحدد في اسم الورقة والرمز الفعلي للفوج.
- تم ضبط **حفظ النقاط التلقائي عند مغادرة الصفحة أو تبديل الأقسام (Zero-Loss Grade Auto-Saving)**: إرسال وحفظ مسودة التقييم والنقاط فوراً عند التبديل بين الأقسام أو الفصول، وعند إلغاء تركيب المكون (Unmount)، وأثناء حدث `beforeunload` لضمان عدم ضياع أي علامة تم إدخالها قبل انقضاء مهلة الـ debounce.
- تم تفعيل **الحفاظ على حضور وسلوك الحصص غير المزامنة (Session Attendance & Behavior Retention)** في `loadCoreState` بحيث لا تؤدي إعادة تحميل الحالة السحابية إلى تصفير سجلات الحضور أو ملاحظات السلوك المحلية المدخلة قبل المزامنة.
- تم اعتماد **معرّفات UUID القياسية المطلقة (Pure UUID v4 Adoption)** في إنشاء الحصص، تقدم المنهاج، والوحدات المخصصة، لتطابق قيود مخطط قاعدة البيانات السحابية وتفادي أي أخطاء تحويل أو عدم توافق في المفاتيح الخارجية.
- تم تفعيل **التفريغ الاستباقي لطابور المزامنة عند الإقلاع (Boot-Time Outbox Flush)** فور اتصال السحابة لإرسال أي تعديلات كانت معلقة أثناء العمل دون اتصال في الجلسة السابقة.
- تم تفعيل **الربط البيداغوجي المباشر بين قسم الحضور ودفتر النصوص (Seamless Attendance-to-Cahier Integration)**:
  - عند توليد حصص الفصل أو إنشاء حصة اليوم في قسم الحضور (`AttendanceSanad`)، يتم تثبيت القسم كفوج نشط تلقائياً (`activeClassId: selectedClassId`).
  - عند النقر على "متابعة في دفتر النصوص" يتم تمرير معرّف الحصة النشطة المحددة (`selectedSessionId`) لتفعيلها وتحميلها مباشرة في نموذج دفتر النصوص (`SessionCahier`).
  - عند حفظ الحصة في الدفتر اليومي، يقوم النظام بتحديث الحصة المولدة ذاتها والحفاظ الكامل على سجل الحضور والغياب وسلوك التلاميذ المسجل دون إنشاء حصص مكررة غير مرتبطة.
  - إضافة إمكانية توثيق وتعديل الحصص المولدة مباشرة من سجل الحصص السابقة مع إظهار شارة "بانتظار التوثيق في الدفتر"، مع التحميل التلقائي لكامل المنهاج والتحديد الذكي للوحدة التعلمية التالية غير المنجزة.
- تم حل مشكلة **اختفاء البيانات وتصفيرها بعد الخروج وإعادة الدخول (Zero-Data-Loss Authentication & Roster Persistence)**:
  - **تصحيح قيد التعارض السحابي (`onConflict: 'id'`)**: تصحيح استهداف القيد في الدفعة السحابية `commitRosterImportBatch` ومخطط قاعدة البيانات `import_roster_batch` من `workspace_id,id` (الذي كان يسبب خطأ PostgreSQL 42P10 وفشل حفظ الأقسام والتلاميذ بنسبة 100%) إلى المفتاح الأساسي الصريح `id`.
  - **معالجة التعارض والتنظيف غير الهدام للتلاميذ**: الحفاظ على التلاميذ الحاليين وعدم حذفهم عشوائياً عند الاستيراد لحماية العلامات وسجلات الحضور المرتبطة بهم من الحذف المتسلسل (`ON DELETE CASCADE`)، مع إزاحة مؤقتة للترقيم لتفادي تعارض القيد الفريد `(workspace_id, class_id, number_in_list)`.
  - **تنظيف الشواهد السابقة (`sync_tombstones`)**: إزالة أي شواهد حذف قديمة للأفواج والتلاميذ المستوردين لمنع تجمد طابور المزامنة بسبب `SyncConflictError`.
  - **القضاء التام على سباق الإقلاع (Startup Race Condition Elimination)**: دمج تحميل الكاش المحلي من IndexedDB مع اتصال السحابة في مسار تهيئة موحد ومتسلسل؛ يُحمّل الكاش المحلي ويُعرض فوراً، ثم تُجلب البيانات السحابية.
  - **قبول القوائم الصفرية المشروعة ومنع قيامة المحذوفات (Definitive Roster Deletion & Zero-Resurrection)**: تم تصحيح سلوك `saveAppStateCache` في `lib/state-cache.ts` لضمان عدم حظر تصفير الأقسام أو حذف آخر فوج، حيث يحظر الكاش فقط استبدال البيانات الحقيقية بالبيانات التجريبية (`isDemoState`). وفي `useCloudAppState.ts`، تم استبعاد استرجاع الكاش القديم عند عودة السحابة بـ 0 أقسام إلا إذا كانت هناك عمليات outbox معلقة تخص المالك، مما ضمن ثبات الحذف وعدم عودة الأقسام المحذوفة عند إعادة تحميل الصفحة.
  - **ربط الاستيراد الذري بسياق التطبيق (`commitRosterImport`)**: دمج عمليات الاستيراد من الرقمنة والممتاز مباشرة عبر `AppStateContext` لتحديث الحالة والكاش و`lastSyncedStateRef` وتنظيف طابور Outbox من التكرارات غير الضرورية.
- تم **التدقيق الشامل والتحصين الكامل ضد أخطاء التعارض في قاعدة البيانات (Zero DB Constraint Violations)**:
  - **التوفيق الاستباقي للكيانات حسب المفاتيح الفريدة (Proactive Unique Key Collision Reconciliation)** في `applyOperationOnce` لكافة الجداول الـ 18:
    - جدول العلامات `grades`: الكشف عن السجلات الحالية للمطابقة `(workspace_id, student_id, trimester)` وإعادة استخدام المعرّف لمنع انتهاك القيد الفريد `grades_workspace_id_student_id_trimester_key`.
    - جدول الحصص `sessions`: الكشف عن الحصص الحالية للمطابقة `(workspace_id, class_id, session_date, start_time)` وإعادة استخدام المعرّف لمنع انتهاك `sessions_workspace_id_class_id_session_date_start_time_key`.
    - جدول الحضور `attendance`: الكشف عن سجل الحضور الحالي `(workspace_id, session_id, student_id)` وإعادة استخدام المعرّف لمنع انتهاك `attendance_workspace_id_session_id_student_id_key`.
    - جدول استعمال الزمن `timetable_slots`: الكشف عن الحصة الأسبوعية `(workspace_id, class_id, weekday, start_time)` وإعادة استخدام المعرّف لمنع انتهاك `timetable_slots_workspace_id_class_id_weekday_start_time_key`.
    - جدول تقدم المنهاج `lesson_progress`: الكشف عن تقدم الوحدة `(workspace_id, class_id, unit_key)` لمنع انتهاك `lesson_progress_workspace_id_class_id_unit_key_key`.
    - جدول مهام اللوحة `dashboard_tasks`: الكشف عن المهمة `(workspace_id, task_id)` لمنع انتهاك `dashboard_tasks_workspace_id_task_id_key`.
    - جدول مساحات العمل `workspaces`: تحويل جميع عمليات الإنشاء في `workspaceId` و`commitRosterImportBatch` إلى `upsert` مع `onConflict: 'owner_id'` لمنع خطأ `23505 duplicate key value violates unique constraint "workspaces_owner_id_key"` في حالات التزامن.
    - جدول المذكرات `memoranda_files`: إزالة حصر البحث بالملفات غير المحذوفة وتدوير السجلات السابقة مع تحديث بياناتها لمنع تعارض الفهرس الفريد الجزئي `memoranda_files_workspace_unit_key_idx`.
    - استيراد القوائم `commitRosterImportBatch`: مطابقة وتوفيق التلاميذ تلقائياً برقم التسجيل أو الترتيب في الفوج `(class_id, number_in_list)` وإعادة استخدام معرّفاتهم السحابية لتفادي التكرار وحماية كافة نقاطهم وحضورهم التاريخي.
- تم **تطبيق مخرجات تدقيق المراجعة الشاملة (Production Review Remedies)**:
  - **تطهير الهوية الافتراضية وحساب الحجم الساعي (Identity Sanitization & Hours Precision)**: تفريغ البيانات الافتراضية الخاصة بالمطور من `DEFAULT_PROFILE` بـ `lib/storage.ts`، تصحيح الاسم الاحتياطي والمؤسسة في `SidebarSanad.tsx` من ("أستاذ محمد" و"ثانوية النخبة") إلى "أستاذ المادة" و"المؤسسة التعليمية"، حساب الحجم الساعي الأسبوعي بدقة وفق `getWeeklyHours(c.level)` بدلاً من ضرب عدد الأقسام في 2 (مراعاة لساعة واحدة لقسم 1 ج.م.علوم)، تصحيح سنوات الخبرة الاحتياطية إلى 0، توحيد السنة الدراسية الاحتياطية لجميع الوثائق والتصديرات إلى "2026/2027"، واستبدال النصوص المؤقتة التوضيحية بنصوص عامة وتصحيح `htmlFor`/`id` في النماذج.
  - **إحكام دوال المهاجرة والقيود العلائقية (Database Schema Hardening)**: تفعيل الهجرة `20260923203000_audit_fixes_and_ledger.sql` بتصحيح دالة `upsert_grade` للبحث عن مساحة العمل بواسطة `id`، وفرض المفاتيح الخارجية المركبة `(workspace_id, owner_id)` على `dashboard_tasks` و`sync_operations` لترتبط بـ `workspaces(id, owner_id) ON DELETE CASCADE`، وإحكام دالة `claim_sync_operation` ومنع وصول الأدوار غير المصادق عليها، وتحديث نطاق مسح العمليات في `clear_roster_data` ليشمل فقط عمليات استيراد القوائم، وإضافة `sync_tombstones` إلى `supabase_realtime`، وتزويد المفاتيح الخارجية بفهارس أداء مستقلة.
  - **تحسينات سهولة الوصول وتجربة المزامنة (UX, a11y, Realtime & CI)**: اعتماد السحابة مصدراً وحيداً للحقيقة عند استقبال تحديثات Realtime دون طابور معلق حتى لو أصبحت القوائم صفرية، إيقاف وميض زر إعادة المحاولة أثناء الإرسال العادي، تصحيح تقديرات النتائج البيداغوجية، تزويد عناصر التحكم وأزرار الحضور والسلوك والنقاط ومربعات الحوار بوسوم `aria-label` و`aria-pressed` المناسبة، حظر القيم الشاذة للعلامات خارج 0-20 بإشعار Toast صريح، وإنشاء مسار GitHub Actions CI للتحقق التلقائي من البناء والاختبارات وفحص الكود.
- تم **التدقيق المعمق والقضاء الجذري على مشاكل المزامنة وفقدان البيانات (Comprehensive Zero-Loss Cloud Sync Architecture)**:
  - **عزل كاش IndexedDB للمستخدمين وتطهير الجلسات (User-Scoped App State Cache & Clean Logout)**: تم حصر مفاتيح كاش الحالة في IndexedDB بمعرّف المستخدم الفريد `sanad:app-state:v4:${userId || 'anon'}`، مع تفعيل تنظيف الكاش عند تسجيل الخروج عبر `clearAppStateCache(userId)`، وتأمين المفتاح `key={user?.id || 'anon'}` على مكون `<AppContent />` لإعادة التهيئة الكاملة والنظيفة لمكونات وخطافات React عند تبديل الحساب أو الخروج.
  - **القضاء التام على الحذف الشبح عند إعادة تحميل الصفحة (Phantom Delete Elimination on Refresh)**: وضع حارس صريح `if (state === lastSyncedStateRef.current) return;` على تأثير الحفظ في الخلفية، مما منع احتساب تحميل البيانات السحابية كتعديل محلي ومنع توليد عمليات حذف وهمية للحصص والأفواج والجدول الزمني عند تحديث الصفحة (F5).
  - **الحفظ الذري الفوري في `updateStateAndWait` (Instant Direct Flush Persistence)**: استبدال استطلاع Polling ذي المهلة 2.5 ثانية بدفع ذري وتفريغ مباشر وفوري لطابور المزامنة `flushSyncOutbox` فور تعديل بيانات البروفايل أو استعمال الزمن أو التلاميذ، مما خفض زمن التأكيد إلى أقل من 300ms وقضى نهائياً على بقاء إشعار "قيد المزامنة" عالقاً على الشاشة.
  - **تصحيح فحص التعارض السحابي `conflictIfStale` (False Conflict Elimination)**: تصحيح مقارنة مراجعة الطفرات `sync_revision` لتتم مع `metadata.revision` المسجلة عند آخر مزامنة للمستخدم، وتجاهل عداد التريجر الداخلي للجدول في PostgreSQL الذي يتزايد داخلياً، مما أوقف الأخطاء الكاذبة لتعارض المزامنة `SyncConflictError`.
  - **استبقاء القسم النشط `activeClassId` وتفادي الشاشات الفارغة**: توفيق استرجاع `activeClassId` في `loadCoreState` بحيث يعتمد أولاً على ما حفظه المستخدم في `app_settings.settings.activeClassId`، أو أول قسم متوفر في السحابة، لمنع إرجاع `null` واختفاء بيانات جداول الأقسام عند الدخول من جلسة جديدة.
  - **نقاء دورة حياة المكونات ومطابقة معايير React 19 (Zero Module Leaks & ESLint Conformity)**: إزالة كافة المتغيرات العامة المشتركة على مستوى الملف (`sharedAppState`, `sharedCloudStatus`, `sharedLastSyncedState`, `sharedRevision`, `sharedUserId`) التي كانت تسبب تعارضات بين الجلسات وانتهاكاً لقواعد React Compiler، والاعتماد الحصري على مراجع React النظيفة وحالات الخطافات المعزولة.
  - **ثبات حفظ النقاط والتقييم ومنع سباق الكتابة (Zero-Race Grade Auto-Saving)**: تصحيح تنظيف المؤقت في خطاف حفظ النقاط بـ `GradesAndEvaluation.tsx`، لمنع إطلاق عمليات حفظ متزامنة متداخلة عند كل ضغطة زر أثناء إدخال العلامات، وحصر الحفظ الفوري عند انتهاء مهلة الـ debounce (900ms) أو عند مغادرة الصفحة (`beforeunload`/`unmount`) وتغيير القسم أو الفصل، مما حمى النقاط من التراجع أو الاستبدال أثناء الكتابة السريعة.
  - **مطابقة حقل الولاية مع مخطط قاعدة البيانات (`state_name`)**: تصحيح تحديث عمود الولاية في `profiles` من `wilaya` إلى `state_name` في `commitRosterImport` لتفادي أخطاء SQL ورفض استيراد القوائم وتحديث الملف الشخصي.
  - **تسريع وتحصين ربط الكيانات البيداغوجية بالأقسام (`O(1) classMatches`)**: تحويل فحص انتماء التلاميذ والنقاط والحصص واستعمال الزمن وتقدم المنهاج إلى بنية `Set` بالمعرّفات الصريحة والمستقرة، مما وفر بحثاً فورياً `O(1)` وضمن عدم استبعاد أي تلميذ أو نقطة أو حصة عند استرجاع البيانات من السحابة.
  - **إصلاح سباق الحفظ الفوري للحالة (`updateStateAndWait`)**: حساب الحالة الجديدة وتحديث المرجع المحلي قبل بدء تفريغ الـ outbox، بدلاً من الاعتماد على callback مؤجل لـ React؛ يضمن ذلك إنشاء عمليات `session` و`timetable` و`attendance` الفعلية عند التعديل، مع بقاء revision وoperation id وtombstones وآلية التعارض دون تغيير. كما تُطلب هوية الصف المُعاد من Supabase بعد كل `upsert` حتى لا يُعلن نجاحاً دون تأكيد الكتابة.
<<<<<<< ours

## عقد سلامة البيانات (Data-Safety Contract) — 2026-09-24

القاعدة الملزمة التي تحكم كل تعديل على التخزين والمزامنة:

1. **Supabase هو المصدر الوحيد للحقيقة.** IndexedDB كاش محلي + طابور عمليات *غير مؤكدة* فقط.
2. **الحذف مطلق.** لا يجوز إعادة إنشاء صف محذوف إلا بقرار صريح من المستخدم.
3. **لا تُحذف عملية من الـ outbox قبل الإقرار**، ولا يُحذف كاش المستخدم قبل تفريغ الطابور أو
   موافقة المستخدم.

### التغييرات المرتبطة (Phase 1)

| الحالة | السلوك الجديد | الملفات | الاختبار |
| --- | --- | --- | --- |
| تحميل الحالة | `loadCoreState` يعتمد السحابة، ويستبقي السجل المحلي **فقط** إذا كان له عمل معلّق في الـ outbox (`pendingRecordIds`) | `lib/supabase/core-sync.ts` | `__tests__/data-safety.test.ts` §1 |
| شواهد الحذف | يقرأ `sync_tombstones` للـ workspace ويسقط أي سجل محلي له شاهد، حتى لو كان الكاش أحدث | `lib/supabase/core-sync.ts` | §2 |
| تعديل سجل محذوف | يُرفض بـ`SyncConflictError` ولا يُمسح الشاهد؛ التجاوز فقط عبر `allowTombstoneOverride` الناتج عن قرار المستخدم في نافذة التعارض | `core-sync.ts`, `lib/sync-outbox.ts`, `hooks/useCloudAppState.ts` | §2 |
| شواهد الحذف عبر الأجهزة | لا يُعاد وسم شاهد حذف جهاز آخر على أنه لهذا الجهاز | `core-sync.ts` | §2 |
| الحضور والسلوك | يُعاد بناؤهما من الجداول العلائقية، ويُحتفظ فقط بالعلامات المعلّقة في الـ outbox | `core-sync.ts` | §6 |
| الحفظ دون اتصال (`local-only`) | التعديلات تُدخل الـ outbox وتُعاد المحاولة تلقائياً بدل بقائها في الذاكرة فقط | `hooks/useCloudAppState.ts` | §3، §6 في `SYNC-REVIEW.md` |
| تسجيل الخروج | نافذة تأكيد عند وجود عمل غير مزامن (مزامنة الآن / تصدير نسخة / خروج)، تفريغ الطابور، ثم `purgeLocalUserData` | `components/SignOutDialog.tsx`, `app/page.tsx`, `lib/local-user-data.ts` | §3 |
| طابور المذكرات | مربوط بالمالك `sanad:memoranda-outbox:<ownerId>:*`، ولا يُفرَّغ إلا لصاحب الجلسة الحالية، مع تبنٍّ مرة واحدة للمفاتيح القديمة | `lib/supabase/memoranda-outbox.ts`, `memoranda-storage.ts` | §3 |
| ملفات PDF المحلية | مفاتيح مقيدة بالمالك `sanad:pdf:<ownerId>:<unitId>` مع ترحيل تلقائي للمفاتيح القديمة | `lib/binary-storage.ts`, `components/LessonPreparation.tsx`, `lib/backup-archive.ts` | §5 |
| ملفات المذكرات في الكاش | تُدمج البيانات السحابية *داخل* السجل المحلي فلا يضيع `fileStorageKey` | `core-sync.ts` | §4 |
| إعادة المزامنة الكاملة | زر في الإعدادات (مع تأكيد) يمسح الطابور ويعيد تحميل مساحة العمل من السحابة | `hooks/useCloudAppState.ts`, `components/SettingsSanad.tsx` | §4 |

### المرحلة الثانية (Phase 2) — انتشار الحذف الفوري وحماية السباقات

| الحالة | السلوك | الملفات | الاختبار |
| --- | --- | --- | --- |
| حذف فوري عبر الأجهزة | `replica identity full` على كل جدول منشور + الاشتراك في `sync_tombstones`، فيصل حدث الحذف إلى كل تبويب مفتوح بدل انتظار إعادة التحميل | `supabase/migrations/20260924120000_realtime_delete_propagation.sql` | `__tests__/realtime-phase2.test.ts` §1 |
| تجاهل صدى الكتابات الخاصة | تُتجاهل أحداث هذا الجهاز نفسه (`sync_device_id` و`device_id` في الشواهد) فلا يستدعي كل حفظٍ إعادة تحميل | `lib/realtime-guard.ts` (`isSelfAuthoredChange`) | §2 |
| سباق الطباعة (Clobber) | طلب تحديث بدأ قبل تعديل المستخدم لا يكتب فوقه: عدّاد تعديلات متزامن + إعادة فحص كامل بعد `await` | `hooks/useCloudAppState.ts`, `shouldApplyRemoteRefresh` | §3 |
| إغلاق التبويب | عند `pagehide`/`visibilitychange:hidden` تُكتب التعديلات المعلّقة في الـ outbox أولاً ثم تُحاول مزامنة واحدة سريعة | `hooks/useCloudAppState.ts` | §4 |
| ساعة المراجعة | تُبدأ من آخر `cloudRevision` معروف ولا ترجع للخلف أبداً، فلا تُستبدل صفوف سحابية بصمت | `lib/realtime-guard.ts` (`nextRevisionFloor`) | §5 |
| الـ service worker | `NetworkOnly` لكل طلبات Supabase ومسارات `/auth/` قبل `defaultCache`، فلا يُخدَّم صف محذوف من كاش HTTP لمدة ساعة | `app/sw.ts` | §6 |

ملاحظة أمنية موثّقة في الهجرة: لا تُطبَّق سياسات RLS على أحداث DELETE، ولذلك فإن مرشّح
الاشتراك (`owner_id=eq.<uid>`) هو ما يحدد نطاق البث، ويُطبَّق داخل الخادم قبل الإرسال.
||||||| base
=======

## عقد سلامة البيانات (Data-Safety Contract) — 2026-09-24

القاعدة الملزمة التي تحكم كل تعديل على التخزين والمزامنة:

1. **Supabase هو المصدر الوحيد للحقيقة.** IndexedDB كاش محلي + طابور عمليات *غير مؤكدة* فقط.
2. **الحذف مطلق.** لا يجوز إعادة إنشاء صف محذوف إلا بقرار صريح من المستخدم.
3. **لا تُحذف عملية من الـ outbox قبل الإقرار**، ولا يُحذف كاش المستخدم قبل تفريغ الطابور أو
   موافقة المستخدم.

### التغييرات المرتبطة (Phase 1)

| الحالة | السلوك الجديد | الملفات | الاختبار |
| --- | --- | --- | --- |
| تحميل الحالة | `loadCoreState` يعتمد السحابة، ويستبقي السجل المحلي **فقط** إذا كان له عمل معلّق في الـ outbox (`pendingRecordIds`) | `lib/supabase/core-sync.ts` | `__tests__/data-safety.test.ts` §1 |
| شواهد الحذف | يقرأ `sync_tombstones` للـ workspace ويسقط أي سجل محلي له شاهد، حتى لو كان الكاش أحدث | `lib/supabase/core-sync.ts` | §2 |
| تعديل سجل محذوف | يُرفض بـ`SyncConflictError` ولا يُمسح الشاهد؛ التجاوز فقط عبر `allowTombstoneOverride` الناتج عن قرار المستخدم في نافذة التعارض | `core-sync.ts`, `lib/sync-outbox.ts`, `hooks/useCloudAppState.ts` | §2 |
| شواهد الحذف عبر الأجهزة | لا يُعاد وسم شاهد حذف جهاز آخر على أنه لهذا الجهاز | `core-sync.ts` | §2 |
| الحضور والسلوك | يُعاد بناؤهما من الجداول العلائقية، ويُحتفظ فقط بالعلامات المعلّقة في الـ outbox | `core-sync.ts` | §6 |
| الحفظ دون اتصال (`local-only`) | التعديلات تُدخل الـ outbox وتُعاد المحاولة تلقائياً بدل بقائها في الذاكرة فقط | `hooks/useCloudAppState.ts` | §3، §6 في `SYNC-REVIEW.md` |
| تسجيل الخروج | نافذة تأكيد عند وجود عمل غير مزامن (مزامنة الآن / تصدير نسخة / خروج)، تفريغ الطابور، ثم `purgeLocalUserData` | `components/SignOutDialog.tsx`, `app/page.tsx`, `lib/local-user-data.ts` | §3 |
| طابور المذكرات | مربوط بالمالك `sanad:memoranda-outbox:<ownerId>:*`، ولا يُفرَّغ إلا لصاحب الجلسة الحالية، مع تبنٍّ مرة واحدة للمفاتيح القديمة | `lib/supabase/memoranda-outbox.ts`, `memoranda-storage.ts` | §3 |
| ملفات PDF المحلية | مفاتيح مقيدة بالمالك `sanad:pdf:<ownerId>:<unitId>` مع ترحيل تلقائي للمفاتيح القديمة | `lib/binary-storage.ts`, `components/LessonPreparation.tsx`, `lib/backup-archive.ts` | §5 |
| ملفات المذكرات في الكاش | تُدمج البيانات السحابية *داخل* السجل المحلي فلا يضيع `fileStorageKey` | `core-sync.ts` | §4 |
| إعادة المزامنة الكاملة | زر في الإعدادات (مع تأكيد) يمسح الطابور ويعيد تحميل مساحة العمل من السحابة | `hooks/useCloudAppState.ts`, `components/SettingsSanad.tsx` | §4 |

### المرحلة الثانية (Phase 2) — انتشار الحذف الفوري وحماية السباقات

| الحالة | السلوك | الملفات | الاختبار |
| --- | --- | --- | --- |
| حذف فوري عبر الأجهزة | `replica identity full` على كل جدول منشور + الاشتراك في `sync_tombstones`، فيصل حدث الحذف إلى كل تبويب مفتوح بدل انتظار إعادة التحميل | `supabase/migrations/20260924120000_realtime_delete_propagation.sql` | `__tests__/realtime-phase2.test.ts` §1 |
| تجاهل صدى الكتابات الخاصة | تُتجاهل أحداث هذا الجهاز نفسه (`sync_device_id` و`device_id` في الشواهد) فلا يستدعي كل حفظٍ إعادة تحميل | `lib/realtime-guard.ts` (`isSelfAuthoredChange`) | §2 |
| سباق الطباعة (Clobber) | طلب تحديث بدأ قبل تعديل المستخدم لا يكتب فوقه: عدّاد تعديلات متزامن + إعادة فحص كامل بعد `await` | `hooks/useCloudAppState.ts`, `shouldApplyRemoteRefresh` | §3 |
| إغلاق التبويب | عند `pagehide`/`visibilitychange:hidden` تُكتب التعديلات المعلّقة في الـ outbox أولاً ثم تُحاول مزامنة واحدة سريعة | `hooks/useCloudAppState.ts` | §4 |
| ساعة المراجعة | تُبدأ من آخر `cloudRevision` معروف ولا ترجع للخلف أبداً، فلا تُستبدل صفوف سحابية بصمت | `lib/realtime-guard.ts` (`nextRevisionFloor`) | §5 |
| الـ service worker | `NetworkOnly` لكل طلبات Supabase ومسارات `/auth/` قبل `defaultCache`، فلا يُخدَّم صف محذوف من كاش HTTP لمدة ساعة | `app/sw.ts` | §6 |

ملاحظة أمنية موثّقة في الهجرة: لا تُطبَّق سياسات RLS على أحداث DELETE، ولذلك فإن مرشّح
الاشتراك (`owner_id=eq.<uid>`) هو ما يحدد نطاق البث، ويُطبَّق داخل الخادم قبل الإرسال.

### المرحلتان الثالثة والرابعة (Phase 3 + 4) — الكفاءة والواجهة المنتجية

| الحالة | السلوك | الملفات | الاختبار |
| --- | --- | --- | --- |
| قراءة الطابور | لا يُقرأ من IndexedDB إلا طابور المالك الحالي (`<ownerId>:` بادئة المعرّف)، مع مسار احتياطي ضيّق لإدخالات uuid القديمة | `lib/sync-outbox.ts` | `__tests__/acceptance.test.ts` |
| فحص الجلسة | ذاكرة مؤقتة 30 ثانية لـ`auth.getUser()` بدل نداء في كل مزامنة/تحديث، وتُبطَل عند الخروج | `hooks/useCloudAppState.ts` | — (سلوك وقت تشغيل) |
| أحداث Realtime | تجميع الطلبات المتلاحقة في تحميل واحد بعد 250ms هدوء بدل تحميل لكل صف | `lib/coalescer.ts` | `__tests__/sync-status.test.ts` §8 |
| لوحة المزامنة | الحالة + عدد العمليات المعلّقة + آخر تأكيد + معرّف الجهاز + «مزامنة الآن» | `components/SettingsSanad.tsx`, `lib/sync-status.ts` | §1–§3 |
| استرجاع عند الإقلاع | رسالة تقدّم بعدد الأقسام والتلاميذ بدل شاشة تحميل صامتة | `app/page.tsx` | §7 |
| مذكرات دون إنترنت | زر «حفظ نسخة للعمل دون إنترنت» + بيان الوحدات غير المحفوظة على الجهاز | `components/LessonPreparation.tsx`, `lib/binary-storage.ts` | §6 |
| تذكير النسخ الاحتياطي | تذكير أسبوعي لكل حساب قابل للتأجيل، ويبقى بعد تطهير البيانات المحلية | `lib/backup-reminder.ts` | `__tests__/acceptance.test.ts` |
| تحصين Excel | حدود على عدد الأوراق والأسطر والزمن أثناء تحليل ملف غير موثوق | `lib/excel-sync.ts` | `__tests__/acceptance.test.ts` |
| الهجرات | إزالة التكرار في هجرتَي استيراد القوائم (الأولى هي المرجع والثانية no-op) | `supabase/migrations/` | `__tests__/acceptance.test.ts` |
| CI | `npm ci` + `tsc --noEmit` + lint + tests + build مع concurrency | `.github/workflows/ci.yml` | `__tests__/acceptance.test.ts` |

### قرارات ما بعد المرحلة الرابعة (2026-09-24)

| البند | القرار | المبرر |
| --- | --- | --- |
| §5.1 توحيد قواعد الدمج | ✅ نُفِّذ | كل قرارات الدمج صارت في `lib/sync-reconcile.ts` (`reconcileEntityList` و`reconcileSessionMarks`) ويستدعيها كل مسار: التحميل، Realtime، إعادة المحاولة، الاستعادة، وإعادة المزامنة الكاملة |
| §5.13 لوحة التشخيص | ✅ نُفِّذ | عرض `sync_health` بصلاحية `security_invoker = true` حتى لا يتجاوز RLS، يعرض عدد الصفوف وأعلى `revision` و`sync_revision` وعدد الشواهد لكل كيان |
| §5.2 ترقيم الإصدارات لكل صف | ⏸ مؤجَّل بقرار مكتوب | يتطلب تغييراً في المخطط والبروتوكول، ولا يمكن التحقق منه دون بيئة تجريبية وجهازين حقيقيين؛ التصميم المقترح موثّق في `SYNC-REVIEW.md` |
| §5.9 عكس العمليات المدمِّرة | ⏸ دليل تشغيل لا كود | يتطلب نسخاً سحابية (PITR) وهي إعداد على مستوى الخطة، أو مخزناً جديداً للقطات وهو قرار منتج بتكلفة تخزين حقيقية |
>>>>>>> theirs
